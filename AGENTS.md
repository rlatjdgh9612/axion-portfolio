# AXION 작업 완료 및 GitHub 게시 규칙

이 규칙은 저장소 전체에 적용합니다.

## 개발·수정 작업의 완료 조건

- 코드, 스타일, 데이터, 자산 또는 설정을 변경한 모든 작업은 완료 전에 `npm run qa`를 실행합니다.
- QA가 실패하면 원인을 분석하고 요청 범위 안에서 수정한 뒤 전체 QA를 다시 실행합니다.
- 자동 수정은 최대 2회 반복합니다. 해결되지 않으면 임의로 우회하지 않고 원인, 영향 범위, 실패한 검사와 다음 조치를 보고합니다.
- 테스트를 통과하지 않은 결과물은 완료로 보고하거나 GitHub에 게시하지 않습니다.
- 리뷰·설명·진단만 요청받은 작업은 파일을 변경하거나 커밋·푸시하지 않습니다.

## QA 범위

- 정적 검사: ESLint, TypeScript 타입 검사, Next.js QA 전용 프로덕션 빌드
- 런타임 검사: HTTP 오류, 브라우저 콘솔 오류, `pageerror`, 실패한 이미지·CSS·JS·폰트 요청
- UI 검사: Desktop 1440px, Folded Desktop 768px, Mobile 390px
- 테마 검사: 홈·소개·프로젝트의 Light / Dark Theme
- 레이아웃 검사: 가로 넘침, 이미지 깨짐, 스타일시트 누락, 메뉴 열기·닫기
- 프로젝트 검사: 전체 9개, 인턴 1개, 외주 4개, 회사 3개, 개인 1개와 9개 상세 경로
- UI를 변경한 작업은 Playwright 결과에 더해 실제 브라우저에서 관련 페이지와 반응형 경계값을 확인합니다.

## Next.js 실행 환경

- 개발 서버, 일반 빌드, QA 빌드는 서로 다른 `distDir`을 사용합니다.
- `npm run dev`, `npm run build`, `npm run qa` 스크립트를 사용하고 `.next*` 폴더를 직접 공유하지 않습니다.
- 실행 중인 개발 서버가 있더라도 QA 빌드가 개발 화면을 손상하지 않아야 합니다.
- QA 빌드 산출물 자체의 ENOENT·export 캐시 오류가 확인되면 소스 파일을 바꾸기 전에 `.next-qa`만 임시 폴더로 이동해 보존하고 깨끗한 빌드를 1회 재실행합니다.
- macOS 동기화가 `.next-*/types` 생성 파일을 `파일명 2.ts` 형태로 복제한 경우 `npm run qa:sanitize`가 동일 해시 복제본만 시스템 임시 폴더로 보존 이동합니다. 소스·빌드 결과 폴더는 자동 정리 대상에 포함하지 않습니다.

## GitHub 게시

- 사용자는 개발·수정 작업 완료 후 별도 요청이 없어도 QA를 통과한 결과를 GitHub에 커밋·푸시하도록 상시 승인했습니다.
- 사용자가 게시하지 말라고 명시한 경우, 리뷰·설명·진단만 요청한 경우에는 커밋·푸시하지 않습니다.
- 게시 전 `npm run qa:publish`를 통과한 뒤 요청 범위의 파일만 스테이징합니다.
- 관련 없는 사용자 변경은 포함하거나 되돌리지 않습니다.
- 강제 푸시는 하지 않습니다.
- push 후 `npm run qa:published`로 작업 트리, upstream, 로컬·원격 SHA 동기화를 확인합니다.
- GitHub Actions `Quality Gate` 완료 상태를 확인하고 실패하면 로그를 기준으로 요청 범위 안에서 수정·재검증합니다.
- 버전을 변경할 때 `package.json`, `package-lock.json`, README의 현재 버전·버전 표, `CHANGELOG.md`를 함께 갱신합니다.

## 최종 보고

- 변경한 범위
- 실행한 QA와 성공·실패 결과
- 발견하고 수정한 오류
- 남은 경고 또는 미검증 범위
- GitHub 게시 시 버전, 브랜치, 커밋 SHA, 원격 동기화와 Actions 상태

측정 근거 없이 임의의 QA 점수를 작성하지 않습니다.
